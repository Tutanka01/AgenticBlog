FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt requirements_ui.txt ./
RUN pip install --no-cache-dir -r requirements.txt -r requirements_ui.txt

COPY . .

CMD ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
